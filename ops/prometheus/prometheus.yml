global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/interceptor-alerts.yml

scrape_configs:
  - job_name: interceptor-java
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - 172.17.0.1:8080
