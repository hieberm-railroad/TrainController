# firmware-arduino

PlatformIO firmware for RS485-connected turnout and signal nodes.

## Environments

- `turnout_node` for turnout actuator firmware.
- `signal_node` for signal controller firmware.

## Build

```bash
pio run -e turnout_node
pio run -e signal_node
```
